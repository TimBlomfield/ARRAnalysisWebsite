{
  "name": "ARR Analysis Website",
  "description": "A website for the ARR Analysis excel add-in",
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "scripts": {
    "postdeploy": "cd website && npm install && npm run build"
  }
}
