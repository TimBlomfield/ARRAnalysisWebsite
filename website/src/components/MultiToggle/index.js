'use client';


const MultiToggle = ({ selected = 0, options, onSelect, disabled = false }) => {
  return (
    <div style={{ width: '100px', height: '60px', backgroundColor: 'red' }}>

    </div>
  );
};


export default MultiToggle;
